<p id="notice"><%= notice %></p>

<dl class="dl-horizontal">
  <dt>Name:</dt>
  <dd><%= @event.name %></dd>

  <dt>Teamgröße:</dt>
  <dd><%= @event.team_size %>

  <dt>Aktiv:</dt>
  <dd><%= @event.active %></dd>
</dl>

<%= link_to 'Bearbeiten', edit_event_path(@event), :class => 'btn btn-default' %>
<%= link_to 'Übersicht', events_path, :class => 'btn btn-default' %>
